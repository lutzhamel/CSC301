-- pattern matchine of structures

structure Point with
    data x.
    data y.
end

-- construct an instance of the structure
let p = Point(0,7).

-- extract the components of the structure with pattern matching
let Point(a,b) = p.
assert(a==0 and b==7).
