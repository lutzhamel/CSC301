-- compute the factorial

load system "io".
load system "type".

function fact
  with 1 do
    return 1
  orwith n:%integer do
    return n*fact(n-1).
end

let x = tointeger(input("Enter a positive integer: ")).
println ("The factorial of "+x+" is "+(fact x)).
