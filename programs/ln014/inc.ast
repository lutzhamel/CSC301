-- first-class functions

function inc with i do
   return i+1.
end

let foo = inc.  -- foo now holds a function value
let x = foo(1). -- execute the function value with argument 1.
assert (x == 2).

